import React from "react";
import Nav from "../Nav";

const Header = () => {
    return (
        <section className="header">
            <Nav />
        </section>
    );
};

export default Header;
